import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hijibiji.maintenance',
  appName: 'HijibijiMaintenancePortal',
  webDir: 'dist'
};

export default config;
